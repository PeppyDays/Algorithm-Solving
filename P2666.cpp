#include <cstdio>
using namespace std;

int get

int main() {
    int n;
    scanf("%d", &n);

    int status, u, v;
    scanf("%d %d", &u, &v);
    status |= (1 << u);
    status |= (1 << v);






    return 0;
}